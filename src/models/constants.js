'use strict';

const { STATUS_ENABLE, STATUS_DISABLE } = require("../helpers");

const ENABLE_DISABLE = [ STATUS_ENABLE, STATUS_DISABLE ]; 

const STOCK_MODEL = 'Stock';

module.exports = {
	ENABLE_DISABLE,
	STOCK_MODEL,
};
